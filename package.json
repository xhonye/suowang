{
  "name": "suowang",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "SUOWANG is a stable, low-cognitive-load mainline navigator for real life.",
  "scripts": {
    "start": "node scripts/serve.mjs",
    "test": "node --test",
    "check": "node --check src/app.js && node --check scripts/serve.mjs && node --test"
  },
  "engines": {
    "node": ">=20"
  }
}
