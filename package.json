{
  "name": "suowang",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "SUOWANG is a local-first mainline cockpit for real life.",
  "scripts": {
    "start": "node scripts/serve.mjs",
    "install-shortcut": "powershell.exe -NoProfile -ExecutionPolicy Bypass -File scripts/install-shortcut.ps1",
    "test": "node --test",
    "check": "node --check src/app.js && node --check src/api.js && node --check src/view-model.js && node --check scripts/serve.mjs && node --check src/server/config.mjs && node --check src/server/database.mjs && node --check src/server/service.mjs && node --test"
  },
  "engines": {
    "node": ">=22"
  },
  "dependencies": {
    "better-sqlite3": "^13.0.3"
  }
}
