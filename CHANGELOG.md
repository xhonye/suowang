# Changelog

## Unreleased

- 修复页面级滚动结构，在 DevTools 底部停靠、RDP 和小窗口下仍可滚到最底；道路资产不变，仅缩小展示窗口并优先裁去少量顶部天空。
- 问候、日期与正确昼夜图标在首帧同步出现，滚离天空后自动获得雾白半透明顶栏，避免延迟闪烁和内容叠字。
- 统一事项区小型控件：完成、持续和添加使用一致的字体、32px 高度与圆角矩形语言。
- 主线卡移除非当前状态标签，以选中样式和低存在感“当前主线”表达指针，并换用几何居中的 SVG 三点菜单。
- 用户界面的“完成标准”统一调整为“现阶段完成标准”，事项右键生命周期文案统一为“完成事项 / 放弃事项 / 删除事项”。
- 新增持续事项：每日最多记录一次，累计次数以完成按钮旁的低存在感 `↻ N` 呈现；支持撤回今天、完成事项或放弃事项，不引入连续打卡、提醒或奖惩。
- 重构手机驾驶舱：道路背景按当前模式无变形裁切，三个主线槽同排直达，并压缩详情、统一标题色、焦点框和底部安全区。
- 手机端下一步卡片去除 `NEXT STEP` 装饰字并进一步压缩纵向高度。
- 手机事项区改为内容驱动高度，空列表紧凑收起，长列表限制高度并内部滚动。
- 用户心智模型统一为“模式 → 主线 → 事项”，指针统一称“当前主线”和“下一步”。
- “历史”更名为“行迹”，“状态通用 Todo”更名为“其他事项”。
- 事项增加可选“最小一步”，并通过 migration 无损兼容 `0.1.0` 数据库。
- 下一步增加「卡住了？」轻量调整面板，可从步幅、事项、主线和模式四个层次换一种走法。
- 精简事项与主线槽的辅助小字，放大并重新对齐三条道路上的模式入口。
- 模式入口改用完整模式名称并强化尺寸；恢复默认提示语调整为“休息好，才能重新出发。”
- “卡住了？”移至下一步右上角并切换整张卡片，下一步不再显示重复主线来源或参与拖动。

## 0.1.0 — 2026-08-23

首个可长期使用的 SUOWANG 本地版本。

- 固定恢复、工作、生活三个状态及每状态三个主线槽。
- 每状态独立记忆 Current、Priority、状态 Todo 与 cue。
- 支持主线与 Todo 的创建、编辑、拖拽、完成、放弃、历史和安全删除。
- 历史 Todo 可撤回；历史主线可复制为新的独立主线。
- SQLite 本地真源、每日备份、完整导出与整库恢复。
- 完成无边框三岔道路主视觉、桌面驾驶舱和 320px 窄屏布局。
- 提供 Agent 友好的 npm CLI、Windows 双击安装与桌面快捷方式。
