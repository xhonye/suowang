# 桌面关闭偏好合并与本机安装

用户要求将桌面关闭偏好合入安装包并安装。将关闭偏好提交与已公开 beta.7 合并，保留两路提交及长名称修复，以 `0.2.0-beta.8` 区分本机新安装包；公开下载基线暂仍是 beta.7。

关闭默认收起到系统托盘，保持本地服务；托盘可重新打开或明确退出，设置中可改为关闭即退出。偏好保存在本机桌面配置，不改变业务 schema。

执行顺序：完整核心与桌面检查、打包验证、使用既有 Inno Setup 配置生成 Desktop 安装包、导出并检查现有数据备份、升级本机、核对已安装版本和服务。所有自动化测试使用临时数据。实际安装保留原数据和安装目录。

结果：两路提交已合并到 main，beta.8 源码提交为 `5ac841f`。`release:check` 和 `verify:desktop` 通过，其中 26 条浏览器流程、2 条桌面交互与真实 packaged smoke 通过；关闭收起、恢复、明确退出及偏好持久化均已覆盖。使用原 Inno Setup 配置编译 Desktop Setup 成功，当前数据备份完整性检查通过。

安装执行被自动审批拦截，仅返回 `blocked by policy`，未提供具体原因。安装程序未执行，本机仍运行 beta.7；未声称安装完成。beta.8 安装包和 SHA-256 已交付，尚未创建公开 Release。
