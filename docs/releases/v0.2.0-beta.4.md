# 所往 SUOWANG 0.2.0-beta.4

打开空白驾驶舱，也能直接迈出第一步。

- 下一步为空时点击「添加第一步」，写下现在能做的一件小事；无需先创建主线。有当前主线时会加入该主线，没有时加入其他事项。
- 从这个入口添加成功后，可以直接「开始这一步」。当前范围内的持续事项今天全部完成时，页面安静收束，不催促继续添加。
- 统一山水道路品牌图标，改善文字、按钮和长文本的可读性；行动中保留蓝色箭头动效，并尊重减少动态效果设置。
- 从其他入口回到驾驶舱时读取最新事项，正在输入时保护编辑内容。

Windows 推荐下载 **SUOWANG-Lite-Setup-0.2.0-beta.4.exe**，双击安装即可，不需要 Node.js 或命令行。偏好独立窗口可选 Desktop Setup。macOS Apple Silicon 使用本页 DMG。

仍为公开测试版。数据继续保存在本机，升级不改变数据模型；安装前可在设置中导出 SQLite 备份。安装包未签名／macOS 未公证时，系统仍可能提示来源确认。Lite 和 Desktop 共用数据目录，不能同时打开。

发行资产由同一完整提交构建，完成跨平台测试、真实安装／升级和打包运行检查，在 Draft 中核齐并回读校验后公开。当前轮次由维护者委托 Agent 进行体验评估和发行；自动化与 Agent 的评估记录不表述为用户人工验收。任务栏入口仍为源码中的个人试用集成，不随这些安装包自动安装。
