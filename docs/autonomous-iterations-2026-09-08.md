# SUOWANG 自主迭代记录

日期：2026-09-08。授权：用户委托自主观察、设计、实现、验证、体验评估和发布小版本；每三次功能迭代执行一次整理迭代。公开内容仍须经过既有构建、安全、升级和资产完整性验证。

## 起点

- 仓库为 `suowang`，分支 `main`，起始提交 `961e0fc`；远端 main 与之相同。
- 当前公开版经 GitHub 实时核对为 `0.2.0-beta.3`；没有未关闭的公开 Issue。
- 接手时已有 43 个修改或新增文件，涉及已授权的品牌精修、行动动效和 WidBar 个人试用入口。已在本次任务的工作目录逐文件备份并保存二进制 diff；保留这些内容，分开记录来源。
- 原有 102 项单元测试通过。交互观察使用独立临时数据库和 `24763` 端口；自动化沿用测试配置，不连接个人服务。
- 产品主线仍为“模式 → 主线 → 事项”，道路视觉与本地 SQLite 真源保持既有合同。

## 机会与执行顺序

| 轮次 | 现场或代码证据 | 改善目标 | 验收重点 |
| --- | --- | --- | --- |
| 1 | 无数据时下一步要求拖动不存在的事项；手机上添加入口在更下方 | 空态直接引导添加第一步；当天持续事项全部完成后安静收束 | 新用户无需先创建主线即可添加并开始；有当前主线时归入当前主线；不虚构完成事实 |
| 2（观察后决定） | 事项只有右键生命周期菜单，设下一步及移动依赖拖动 | 让触摸和键盘也能完成核心选择 | 重用既有菜单与事务；不增加业务实体；当天已完成持续事项不再推荐 |
| 3（观察后决定） | 原地编辑保存失败后整体重绘，修改内容丢失；输入法 Enter 没有组合输入保护 | 输入和保存反馈可靠 | 错误可修正并重试，中文输入不提前提交，焦点不丢失 |
| 整理 | 三轮形成的重复入口和过期表述 | 删除重复代码、收敛文档与发布记录 | 不删除用户探索档案，不扩张架构，不以无关重构拖延交付 |

每轮只在观察证据仍成立时推进。每轮完成实现、相称回归、三种尺寸体验与发行验证后发布可追溯小版本，再重新观察。候选和公开 Release 分开记录；不能用本地构建替代公开发布，也不能把 Agent 观察表述为用户人工验收。

## 停止条件

核心循环中可复现、低风险、高收益的阻碍已处理，剩余方向需要真实使用反馈或实质产品取舍时停止扩展。功能数量不是进度指标。

## 迭代结果

第 1 轮已实现：空态引导直接进入既有添加框；从该入口添加成功后，焦点回到「开始这一步」。常规列表连续添加保持原行为。当日持续事项完成说明只针对当前主线与其他事项。新增两个真实浏览器流程覆盖三种尺寸、无主线起步和持续事项完成后的空态，初轮通过；完整发行验证进行中。

本地检查：102 项单元、16 项浏览器、14 项桌面单元、1 项桌面完整交互通过；实际 packaged smoke 验证数据库写入及四张批准道路解码，安全检查高风险 blocker 为 0。公开审计发现旧精修记录含本机仓库绝对路径，已改为项目名称后通过；运行时依赖审计 0 告警，构建依赖保留原有三项限时审查风险。WidBar 既有连接测试 5 项及临时服务 smoke 12 项通过，其源码保留个人试用边界。

最终版本号下的完整 `release:check` 已串行通过。此前一次与打包重叠的检查发生桌面服务测试进程异常退出，没有断言栈；单独重跑该文件、桌面单元组及完整串行门禁均通过，Windows 应用崩溃日志没有对应记录。原因未确认，保留失败日志；后续不并行运行打包和测试。

发行证据补齐：Windows/macOS 候选保存真实安装后与旧 schema 升级后的窗口截图和 smoke JSON，作为独立短期 artifact，供发布前实际检查；安装资产、校验和、安全约束和聚合流程保持原要求。本次授权由 Agent 承担体验评估，记录来源不冒认维护者人工操作。

首批 macOS 证据虽然报告成功，截图仍在加载遮罩，未接受为可发布结果。截图采集原先只在隐藏窗口显示后固定等待 300ms；改为检查真实可见状态、遮罩隐藏及透明度，并等待两个绘制帧，超时即失败。窗口以不抢焦点方式显示。重新构建同 SHA 的双平台候选后再审阅。

第 1 轮已公开：[beta.4](https://github.com/xhonye/suowang/releases/tag/v0.2.0-beta.4)，提交 `70364fd6db0959f00617f90fcc2ec6182eca4ac7`。核心 CI `34173631571`、Windows `34173633498`、macOS `34173635556` 全部通过；四张真实安装窗口截图已审阅。聚合发行 `34174206788` 成功，8 个资产均已上传并通过回读校验，Tag 实时核对指向该提交。

第 2 轮证据与设计：实际点击非当前主线事项后收到「下一步只能来自当前主线事项或其他事项」；临时数据库重现当天已完成持续事项仍能设为下一步和开始。增加可见三点入口，重用上下文菜单、移动对话框和既有 API，排除不可选事项，并在服务事务中拒绝当天重复选择／开始。没有新实体、表或依赖。初轮 16 项服务测试通过；菜单三种尺寸、键盘返回、移动保留身份及自动接棒、合法选项过滤的 4 项浏览器测试通过。第一次测试定位器把下拉选项文字也计入标签，改用已正确显示的组合框可访问名称后通过，未放宽业务断言。

第 2 轮本地发行检查通过：103 项单元、20 项浏览器、14 项桌面单元、1 项桌面交互，以及实际打包 smoke、安全与发行审计。Agent 在 320px 窗口实际选择下一步并将事项移入另一主线，检查了 1920×1080、2560×1440 的菜单布局；触发入口、焦点、下一步和移动结果均符合预期。生成 beta.5 双平台候选。

第 3 轮证据与设计：实际重名修改失败后，旧版整体重绘导致草稿与焦点消失。改为仅在成功后结束编辑，失败留在原处；取消时恢复原控件，不做无关全页重绘。中文组合输入不处理 Enter／Escape，提交期间防止重复请求。确认弹窗同样保持到成功才关闭，错误在弹窗内显示；首次新增跨模式复制测试进一步发现 Enter 默认触发了最前方关闭按钮，已将取消控件改为明确的普通按钮。复制返回对应模式并持久化，浏览器恢复先选备份模式再重绘。

第 3 轮初步验证：7 项编辑、断线、复制和恢复浏览器流程通过；三个尺寸的失败输入截图已审阅。Agent 在 320px 窗口实际重现重名失败、保留输入并改正保存，焦点回到原字段。没有新增存储层或业务实体。

第 2 轮已公开：[beta.5](https://github.com/xhonye/suowang/releases/tag/v0.2.0-beta.5)，提交 `a653ba45fe8c8d50fd07f5c075063452ba5b0317`。核心 CI `34174705977`、Windows `34174706372`、macOS `34174708150` 通过；四张真实安装窗口截图已审阅。聚合发行 `34175378739` 成功，8 个资产和不可变 Tag 实时复核通过。一次 macOS artifact 下载连接中断，重新下载成功，未重建或替换候选。

第 3 轮本地发行检查通过：103 项单元、25 项浏览器、14 项桌面单元、1 项桌面完整交互，以及 Forge 打包、实际 smoke、视觉资产、安全和依赖审计。生成 beta.6 双平台候选。随后进入约定的整理迭代，合并重复入口与无引用样式，不扩张产品概念。
